name: Self hosted
on: [push]                            # Triggers the workflow on every push to the repository

jobs:
  # Job name
  self-hosted:
    runs-on: self-hosted              # Github hosted runners
    steps:
      - name: Self hosted runner
        run: echo "Hello, I am from self hosted runner"     # The command to execute